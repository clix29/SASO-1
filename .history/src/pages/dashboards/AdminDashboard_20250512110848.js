import { Typography, Box } from '@mui/material';

const AdminDashboard = () => {
  return (
    <Box>
      <Typography variant="h4">Admin Dashboard</Typography>
      <Typography>Welcome to the administration portal</Typography>
    </Box>
  );
};

export default AdminDashboard;
