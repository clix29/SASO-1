import React from 'react';
import { Box, Typography } from '@mui/material';

const Attendance = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Attendance Review
      </Typography>
      <Typography>
        This is a placeholder page for reviewing attendance records.
      </Typography>
    </Box>
  );
};

export default Attendance;
