import React from 'react';
import { Box, Typography } from '@mui/material';

const Communication = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Communication Panel
      </Typography>
      <Typography>
        This is a placeholder page for the communication panel.
      </Typography>
    </Box>
  );
};

export default Communication;
