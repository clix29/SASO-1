import { Typography, Box } from '@mui/material';

const StudentDashboard = () => {
  return (
    <Box>
      <Typography variant="h4">Student Dashboard</Typography>
      <Typography>Welcome to your student portal</Typography>
    </Box>
  );
};

export default StudentDashboard;
