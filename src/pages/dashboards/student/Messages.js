import React from 'react';
import { Box, Typography } from '@mui/material';

const Messages = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Messages
      </Typography>
      <Typography>
        This is a placeholder page for checking new messages from lecturers and tutors.
      </Typography>
    </Box>
  );
};

export default Messages;
