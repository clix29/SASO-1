import React from 'react';
import { Box, Typography } from '@mui/material';

const Schedule = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Upcoming Schedule
      </Typography>
      <Typography>
        This is a placeholder page for viewing your upcoming classes and tutorials.
      </Typography>
    </Box>
  );
};

export default Schedule;
