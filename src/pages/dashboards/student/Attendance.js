import React from 'react';
import { Box, Typography } from '@mui/material';

const Attendance = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Attendance
      </Typography>
      <Typography>
        This is a placeholder page for tracking your attendance records.
      </Typography>
    </Box>
  );
};

export default Attendance;
